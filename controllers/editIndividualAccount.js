const handleEditIndividualAccount = (req, res, next, postgresDB ) => {
    // post
    //account name, account type? Determine if this will cause issues and probably avoid, low alert balance, and delete
}

module.exports = {
    handleEditIndividualAccount
}